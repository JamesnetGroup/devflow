<ctl:View x:Class="DevFlow.History.Views.HistoryView"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
          xmlns:ctl="clr-namespace:DevFlow.Controls;assembly=DevFlow.Controls"
          mc:Ignorable="d" Style="{StaticResource VIEW}">
    <Border  Background="#aa000000" Padding="10" Margin="200" >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListBox Style="{StaticResource LBX.HISTORY}"/>
            <Image Grid.Column="1" Source="{Binding CurrentImage.ImagePath}"/>
        </Grid>
    </Border>
</ctl:View>
